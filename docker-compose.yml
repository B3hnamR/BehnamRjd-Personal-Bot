version: "3.9"

services:
  bot:
    build: .
    container_name: personal-telegram-bot
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TZ=Asia/Tehran
    volumes:
      - ./data:/app/data
    restart: unless-stopped
